> Доработать подстановку значений по умолчанию в DAO
> Добавить создание alter
> Доработать генерацию констрэйнов
> Доработать порядок полей при построении view (д.б. порядок по словарю!)
> Именование полей для втроенных объектов: нельзя именовать по entry встроенного объекта (иначе два одинаковых объекта нельзя встроить)
> Сделать два режима drop (all/type)
> Entity: добавить объект constraint (включающий ограничения, в т.ч. на null)

  C (check constraint on a table)
  P (primary key)
  U (unique key)
  R (referential integrity)
  V (with check option, on a view)
  O (with read only, on a view)

> Entity: добавить метод merge, для объединения существующей и новой сущностей.
