1) “ип коллбэка теб€ интересовать не должен, так как формат запроса и ответа одинаковый. ¬ажным этот вопрос станет тогда, когда будем делать обертку высокого уровн€ над УHФ, котора€ будет оперировать не тэгами, а их комбинаци€ми. —истема должна полноценного работать и без этой обертки, котора€ по своей сути просто функции shortcutТы дл€ быстрого набора определенных комбинаций.

2) ‘ормат запроса следующий

     Path  =>  :book/callbacks/:id (виды запросов: POST или GET)
     Data  =>  { 
(string, required) path: Сall/12/edit?q=HelloТ, 
(key-value, not required) inputs: { Сa.b.cТ: 12, Сa.b.xТ: 14 }, 
(key-value, not required) params: { } 
     }

3) ќтвет бывает двух типов:
a) просто текст - используетс€ дл€ отрисовки списков в автозаполнени€х или ответах дл€ сторонних сервисов
        б) JSON (key-value) {
   (string) path - сервер автоматически собирает путь на основании значений переменных answer.params : KeyValue
   regions: {
  СregionNameТ: СcontentТ,
СregionNameТ: Сcontent'
   }
 }

4) –егионы должны регистрироватьс€ в объекте book, а также иметь свои контейнеры в HTML, который будет сгенерирован функцией layout того же book. »мена регионов равны значению HTML атрибута id соответствующего контейнера.

Ќапример:

book.regions.regionX = function(a : Answer) : String {
  var something = СHello С + a.params.something;
  return h.p(something);
}

book.layout = h(Сdiv.wrapperТ,
  h(Сdiv#regionX, G(СregionXТ));